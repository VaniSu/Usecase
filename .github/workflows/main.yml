name: First Workflow

on:
  push: 

jobs:
  echo_hello_world:
    runs-on: ubuntu-latest
    steps:
    - name: print hello world
      run: echo "Hello World"

  echo_goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Failed Step
        run: | 
          echo "I will fail" 
          echo "I will fail" 
          echo "I will fail" 
          exit 0

      - name: Say goodbye
        run: 
          echo "Goodbye!" 
          exit 0

     
